# NPM Configuration for Dream Gadgets Monorepo

# Use workspaces
legacy-peer-deps=false

# Ensure consistent versions
save-prefix=^

# Strict SSL only in production
strict-ssl=true

# Use latest npm lockfile format
lockfile-version=2
